<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.haidong.storysharing.dao.RoleMapper">
  <resultMap id="BaseResultMap" type="role">
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="role_name" jdbcType="VARCHAR" property="roleName" />
    <result column="role_label" jdbcType="VARCHAR" property="roleLabel" />
    <result column="is_disable" jdbcType="INTEGER" property="isDisable" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
    <result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
  </resultMap>
  <sql id="Base_Column_List">
    id, role_name, role_label, is_disable, create_time, update_time
  </sql>
  <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List" />
    from tb_role
    where id = #{id,jdbcType=INTEGER}
  </select>

  <select id="getUserRolesByUid" parameterType="int" resultMap="BaseResultMap">
    select role.id, role.role_name, role.role_label, role.is_disable, role.create_time, role.update_time
    from tb_role role,tb_user_role tur
    where tur.user_id = #{uid} and tur.role_id = role.id
  </select>

  <insert id="insertOne">
    insert into tb_user_role (user_id,role_id) values (#{uid},#{rid})
  </insert>

  <delete id="removeOneByUserId">
    delete from tb_user_role where user_id = #{uid}
  </delete>
</mapper>
